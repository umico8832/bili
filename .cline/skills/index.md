# 🤖 BILI 项目全栈 AI 技能路由表

你是本项目的核心 AI 助手。在接到开发需求时，请务必先评估需求类型，并严格按照以下路径**读取对应的规范文件**后再进行开发：

| 需求场景类型 | 需要读取的规范文件路径 | 目标工作目录 |
| :--- | :--- | :--- |
| **开发前端 UI 卡片组件** | `.cline/skills/vue-tailwind-card.md` | `apps/web/src/components/` |

**执行逻辑：**
1. 你的所有前端代码（Vue/Vite/Tailwind）都必须写在 `apps/web/` 目录下。
2. 在开始写代码前，必须使用 `read_file` 读取上述表格中对应的 `.md` 规范。
3. 严格遵守规范中的代码风格和交互细节进行开发。